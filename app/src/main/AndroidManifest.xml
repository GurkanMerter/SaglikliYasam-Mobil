<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dengeliyasam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DengeliYasam">
        <activity android:name=".Activities.SohbetActivity" />
        <activity android:name=".Activities.EgitmenUyelik" />
        <activity android:name=".Activities.KullaniciUyelik" />
        <activity android:name=".Activities.KullaniciEkran" />
        <activity android:name=".Activities.EgitmenGirisActivity" />
        <activity android:name=".Activities.KullaniciGirisActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>